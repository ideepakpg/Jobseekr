@model IEnumerable<Jobseekr.Models.JobListing>

@{
    ViewBag.Title = "LatestJobs";
}

<h2>LatestJobs</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>

<p>
    @Html.ActionLink("View Profile", "ViewProfile", "Jobseekr", new { /* optional route values */ }, new { @class = "btn btn-primary" })
</p>
<p>
    <a href="@Url.Action("ViewJobApplications", "Jobseekr")">View Job Applications</a>

</p>



<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.JobTitle)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Location)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Salary)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PostedDate)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @item.JobTitle
            </td>
            <td>
                @item.Description
            </td>
            <td>
                @item.Location
            </td>
            <td>
                @item.Salary.ToString("N0")
            </td>
            <td>
                @item.PostedDate.ToString("dd-MM-yyyy")
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.JobId }) |
                @Html.ActionLink("Details", "Details", new { id = item.JobId }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.JobId })
            </td>
        </tr>
    }

</table>
